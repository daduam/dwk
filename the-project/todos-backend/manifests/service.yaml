apiVersion: v1
kind: Service
metadata:
  name: the-project-todos-backend-svc
spec:
  type: ClusterIP
  selector:
    app: the-project-todos-backend
  ports:
    - port: 1234
      protocol: TCP
      targetPort: 8081
