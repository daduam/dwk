apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-data-pvc
  namespace: project
spec:
  storageClassName: project-shared-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
