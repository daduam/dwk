apiVersion: v1
kind: Service
metadata:
  name: the-project-todos-backend-svc
  namespace: project
spec:
  type: ClusterIP
  selector:
    app: the-project-todos-backend
  ports:
    - port: 1234
      protocol: TCP
      targetPort: 8081
