apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: the-project-todos-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: the-project-todos-svc
            port:
              number: 1234
